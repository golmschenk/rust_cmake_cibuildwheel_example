[project]
name = "rust_cmake_cibuildwheel_example"
version = "0.1.0"
requires-python = ">=3.8"

[build-system]
requires = ["scikit-build-core==0.6.0"]
build-backend = "scikit_build_core.build"

[tool.cibuildwheel]
only = "cp38-*"
build-verbosity = "3"

[tool.scikit-build]
build-dir = "build"
